@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ViewPizzas</title>
    <style>
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }
    </style>
</head>
<body>
    <div><h2 style="background-color:green;text-align:center;font-size:30px;padding:5px;">Available Pizzas</h2></div>
    <div><h2 style="background-color:aqua;font-size:30px;padding:5px;"><a class="btn btn-outline-danger" asp-controller="Admin" asp-action="CreateProduct">Create New Pizza</a></h2></div>
    <table cellpadding="10px" style="padding-top:30px;">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Image</th>
                <th>Available Quantity</th>
                <th>Category</th>
                <th>Modify</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Price</td>
                    <td><img src="@item.Image" width="100%" height="225" /></td>
                    <td>@item.Quantity</td>
                    <td>@item.CategoryName</td>
                    <td>
                        <a class="btn btn-outline-primary" asp-action="EditCategory" asp-route-id="@item.Id">Edit</a> <br> <br>
                        <a class="btn btn-outline-success" asp-action="CategoryDetails" asp-route-id="@item.Id">Details</a> <br> <br>
                        <a class="btn btn-outline-danger" asp-action="DeleteCategory" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>


